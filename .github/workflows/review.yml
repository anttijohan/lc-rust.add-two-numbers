name: review-pipeline-for-add-two-numbers
on:
  push:

jobs:
  review_pipeline:
    name:    Review (Build, Test)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Build binaries
        run:  cargo build --verbose
      - name: Run tests
        run:  cargo test --verboseb